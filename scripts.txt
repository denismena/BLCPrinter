alter table [dbo].[LIBRARIE] add L_INFO VARCHAR(30) NULL
GO

CREATE TABLE [dbo].[PERSOANE_EXTRA](
	[PE_ID] [int] IDENTITY(1,1) NOT NULL,
	[PE_CONTRACT_ID] [int] NOT NULL,
	[PE_NUME] [varchar](100) NOT NULL,
	[PE_DATA_NASTERII] [datetime] NULL,
	[PE_COPIL] [varchar](1) NULL,
 CONSTRAINT [PK_PERSOANE_EXTRA] PRIMARY KEY CLUSTERED 
(
	[PE_ID] ASC
)WITH (PAD_INDEX = OFF, STATISTICS_NORECOMPUTE = OFF, IGNORE_DUP_KEY = OFF, ALLOW_ROW_LOCKS = ON, ALLOW_PAGE_LOCKS = ON) ON [PRIMARY]
) ON [PRIMARY]

GO

SET ANSI_PADDING OFF
GO

ALTER TABLE [dbo].[PERSOANE_EXTRA]  WITH CHECK ADD  CONSTRAINT [FK_PERSOANE_EXTRA_PERSOANE_EXTRA] FOREIGN KEY([PE_CONTRACT_ID])
REFERENCES [dbo].[CONTRACTE] ([C_ID])
GO

ALTER TABLE [dbo].[PERSOANE_EXTRA] CHECK CONSTRAINT [FK_PERSOANE_EXTRA_PERSOANE_EXTRA]
GO


ALTER TABLE CONTRACTE ADD C_MIJLOC_TRANSPORT	varchar(50)	NULL,
C_ORA_PLECARE	datetime	NULL,
C_LOCUL_PLECARE	varchar(50)	NULL,
C_ORA_SOSIRE	datetime	NULL,
C_LOCUL_SOSIRE	varchar(50)	NULL,
C_ORA_PLECARE2	datetime	NULL,
C_LOCUL_PLECARE2	varchar(50)	NULL,
C_ORA_SOSIRE2	datetime	NULL,
C_LOCUL_SOSIRE2	varchar(50)	NULL,
C_ALTE_MENTIUNI	varchar(MAX)	NULL,
C_AGENTIE	varchar(100)	NULL
GO