@model BLCPrinter.LIBRARIE

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

@using (Html.BeginForm()) {
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>LIBRARIE</legend>

        <div class="editor-label">
            @Html.LabelFor(model => model.L_ID)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.L_ID)
            @Html.ValidationMessageFor(model => model.L_ID)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.L_NUME)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.L_NUME)
            @Html.ValidationMessageFor(model => model.L_NUME)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.L_TIP)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.L_TIP)
            @Html.ValidationMessageFor(model => model.L_TIP)
        </div>

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/scripts/jqueryval")
}
