@model IEnumerable<BLCPrinter.LIBRARIE>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="tbList" style="width:100%">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.L_ID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.L_NUME)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.L_TIP)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.L_ID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.L_NUME)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.L_TIP)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.L_ID }) |
                @Html.ActionLink("Details", "Details", new { id = item.L_ID }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.L_ID })
            </td>
        </tr>
    }

</table>
